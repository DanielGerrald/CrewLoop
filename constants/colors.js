// CrewLoop brand palette — the widely-reused, brand-specific colors.
// Generic colors (white, black) and one-off single-component colors stay
// as local literals in the file that uses them, matching how the example
// project's own Colors constant only holds brand-specific shades.
const Colors = {
  primary: "#1B3A6B", // deep navy — icon backgrounds, primary accents
  accent: "#F47C20", // orange — primary buttons / CTAs
  background: "#1E2530", // near-black blue-grey — app background
  surface: "#6A89A7", // blue-gray — cards, inputs, modals
  muted: "#8A95A3", // muted text, borders, placeholders
  textSecondary: "#D3D3D3",
  success: "#01ab52",
  error: "#FF6B6B",
};

export default Colors;
